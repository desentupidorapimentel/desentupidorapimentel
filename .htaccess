# Habilita o módulo de reescrita
RewriteEngine On

# Define a base URL
RewriteBase /

# Redireciona todas as URLs amigáveis para index.html
# Exemplo: /desentupidora-centro-curitiba -> /index.html
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteCond %{REQUEST_URI} !^/index\.html$
RewriteRule ^(.+)$ /index.html [L]

# Força HTTPS (opcional - descomente se tiver SSL)
# RewriteCond %{HTTPS} off
# RewriteRule ^(.*)$ https://%{HTTP_HOST}%{REQUEST_URI} [L,R=301]

# Remove .html das URLs (opcional)
# RewriteCond %{REQUEST_FILENAME}.html -f
# RewriteRule ^(.*)$ $1.html [L]
